# Лабораторная работа №4
## Написание десктопного приложения на Windows Forms

# Банковская система "JokeBank45"

Проект представляет собой десктопное приложение с графическим интерфейсом для управления банковскими вкладчиками.

## 🎯 Функциональность

### Основные возможности:
- **Добавление вкладчиков** - создание новых клиентов банка
- **Управление вкладами** - редактирование данных и размера вкладов
- **Удаление вкладчиков** - полное удаление клиентов из системы
- **Просмотр информации** - табличное отображение всех вкладчиков
- **Импорт данных** - загрузка вкладчиков из текстовых файлов
- **Экспорт данных** - сохранение информации в файлы

## 🏗️ Архитектура

### Основные классы:

#### `Contributor` (Модель данных C++)
- Хранит информацию о вкладчике в нативном C++
- Свойства: name, age, size
- Методы для доступа к данным и отображения

#### `ManagedContributor` (Управляемая модель данных)
- Обертка для C++ класса в C++/CLI
- Свойства: Name, Age, Size
- Мост между нативным кодом и .NET

#### `DataManager` (Менеджер данных)
- Singleton для управления коллекцией вкладчиков
- CRUD операции с данными
- Централизованное хранение состояния приложения

### Графические формы:

#### `MyForm` (Главное окно)
- Центральный навигационный узел
- Кнопки доступа ко всем функциям системы
- Отображение данных в DataGridView

#### `MyForm2` (Добавление вкладчиков)
- Выбор способа добавления (ручной/импорт)
- Навигация к формам ввода

#### `MyForm3` (Управление вкладчиками)
- Табличное отображение всех вкладчиков
- Функция удаления выбранных записей

#### `AddConMan` (Ручной ввод)
- Форма для добавления новых вкладчиков
- Валидация вводимых данных в реальном времени

#### `EditContributorForm` (Редактирование)
- Изменение данных существующих вкладчиков
- Обновление имени, возраста и размера вклада

## 🚀 Использование

### Главное меню:
1. **Add a contributor** - переход к добавлению вкладчиков
2. **Delete a contributor** - управление и удаление вкладчиков
3. **Show information** - просмотр всех данных в таблице
4. **Total amount** - общая сумма всех вкладов
5. **Save to txt** - экспорт данных в файл
6. **Exit** - выход из приложения

### Процесс работы:
1. Добавление вкладчиков через ручной ввод или импорт из файла
2. Просмотр и редактирование данных через табличный интерфейс
3. Управление вкладчиками (удаление ненужных записей)
4. Экспорт данных для резервного копирования

## 💡 Особенности реализации

### Валидация данных:
- **Имя**: 1-10 символов, обязательное поле
- **Возраст**: 18-120 лет, только цифры
- **Размер вклада**: положительное число
- **Визуальная обратная связь**: цветовая подсветка полей

### Паттерны проектирования:
- **Singleton** - для менеджера данных
- **MVC-архитектура** - разделение интерфейса, логики и данных
- **Bridge Pattern** - связывание C++ и C++/CLI кода

## 🛠️ Технические детали

### Технологии:

#### **C++/CLI**
- Основной язык разработки приложения
- Позволяет сочетать нативный C++ с .NET Framework
- Обеспечивает прямой доступ к Windows API и .NET библиотекам
- Используется для создания Windows Forms интерфейса

#### **Windows Forms**
- Графический фреймворк для создания UI
- Быстрое прототипирование интерфейсов
- Встроенные контролы: DataGridView, TextBox, Button, Label
- Поддержка событийной модели программирования

#### **.NET Framework**
- Платформа для запуска приложения
- Поддержка сборок и пространств имен
- Automatic memory management (GC)
- Библиотеки для работы с файлами и IO

## 📋 Структура проекта

### Основные файлы:

#### **Contributor.h**
```cpp
class Contributor {
private:
    string name;
    int age;
    double size;
public:
    // Конструкторы, геттеры, сеттеры
    void displayContributor() const;
};
```

#### **DataManager.h**
```cpp
public ref class DataManager sealed {
private:
    static DataManager^ _instance;
    List<ManagedContributor^>^ contributors;
public:
    static property DataManager^ Instance;
    void AddContributor(String^ name, int age);
    bool RemoveContributor(int index);
    // ... другие методы
};
```

#### **MyForm.h**
- Главная форма приложения
- Навигация между формами
- Отображение данных в DataGridView

## 🔧 Функциональные модули

### Модуль добавления вкладчиков:
- Ручной ввод с валидацией
- Импорт из текстовых файлов
- Автоматическая обработка различных форматов данных

### Модуль управления данными:
- Табличное представление
- Поиск и фильтрация
- Редактирование записей
- Удаление с подтверждением

### Модуль экспорта данных:
- Сохранение в текстовый формат
- Поддержка различных кодировок
- Резервное копирование информации

## 💾 Обработка данных

### Формат импорта/экспорта:
```
Иван Иванов,25,1000.50
Петр Петров,30,2500.75
Мария Сидорова,28,1500.00
```

### Валидация при импорте:
- Проверка формата строк
- Валидация возраста (18-100 лет)
- Корректность числовых значений
- Обработка ошибок формата

## 🎨 Пользовательский интерфейс

### Дизайн:
- Единый стиль оформления
- Интуитивная навигация
- Визуальная обратная связь
- Адаптивная верстка

### Элементы управления:
- Кнопки с четкими надписями
- Текстовые поля с подсказками
- Таблицы с сортировкой
- Диалоговые окна

## 📝 Особенности реализации

### Межъязыковое взаимодействие:
- Marshaling между std::string и String^
- Преобразование типов данных
- Совместное использование нативного и управляемого кода

### Управление памятью:
- Automatic garbage collection для управляемых объектов
- Явное управление памятью для нативных объектов
- Правильное освобождение ресурсов

### Обработка исключений:
- Try-catch блоки для всех операций ввода-вывода
- Валидация пользовательского ввода
- Гибкая обработка ошибок формата

## 🚀 Запуск и компиляция

### Требования:
- Visual Studio 2019 или выше
- .NET Framework 4.5+
- Поддержка C++/CLI

### Компиляция:
1. Открыть решение в Visual Studio
2. Выбрать конфигурацию сборки (Debug/Release)
3. Скомпилировать проект
4. Запустить приложение

---

*Приложение демонстрирует возможности создания десктопных приложений на C++/CLI с использованием Windows Forms и современных подходов к проектированию пользовательских интерфейсов.*
